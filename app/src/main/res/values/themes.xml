<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тема нужна только на время старта Activity.
         Дальше всё оформление делает Compose (SproutTheme). -->
    <style name="Theme.Sprout" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/sprout_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
